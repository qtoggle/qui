name: CI

on: push

jobs:
  lint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v1.1.0
        with:
          node-version: '10.x'
      - run: echo "this will fail" && false
#      - run: npx eslint js
